version: "3.9"
services:
  web:
    build: ./api
    depends_on:
      - db
    env_file: .env
    ports:
      - "8080:8000"
    volumes:
      - "./api/src:/app"
  db:
    image: mysql:8.0.32
    env_file: .env
    volumes:
      - data-db:/var/lib/mysql

volumes:
  data-db:
